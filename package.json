{
  "name": "downpour",
  "version": "1.0.0",
  "private": true,
  "description": "DOWNPOUR — a multiplayer falling-block game on Cloudflare Workers + Durable Objects.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.20.0"
  }
}
